import { environment } from '../../environments/environment';

export class SiteConstants {
    public static readonly clientId = environment.clientId;
    public static readonly clientSecred = environment.clientSecret;
}
