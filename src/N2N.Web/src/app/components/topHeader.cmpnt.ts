import { Component } from '@angular/core';
import {UserService} from '../services/userService';

@Component({
  selector: 'top-header',
  templateUrl: '../templates/TopHeader.cmpnt.html',
})
export class TopHeaderCmpnt { 
}
